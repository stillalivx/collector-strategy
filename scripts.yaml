unstable: true
allow:
  - net
  - read
  - write
  - env
  - run
scripts:
  collection:
    cmd: "deno run ./src/cli/main.ts"
  compile-cli:
    cmd: "deno compile --output collection --allow-net --allow-env --allow-write --allow-read --allow-run --unstable ./src/cli/main.ts"